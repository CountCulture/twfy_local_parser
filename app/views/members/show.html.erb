<h1><%= @member.full_name %> <em><%= link_to "council page", @member.url %></em></h1>
<p>Council <strong><%= link_to h(@member.council.name), @member.council %></strong></p>
<p>Party <strong><%= @member.party %></strong></p>
<p>Email <strong><%= @member.email ? mail_to(@member.email) : 'not known' %></strong></p>

<h3>Council Committee Memberships</h3>
<% if !@committees.blank? %>
  <ul>
    <% for committee in @committees %>
      <li><%= link_for committee %></li>
    <% end %>
  </ul>
<%- else -%>
  <p>Not a member of any committee</p>
<% end %>

<p class="attribution">Last updated <%= @member.updated_at.to_s(:short) %> (<%= time_ago_in_words(@member.updated_at) %> ago)</p>

